import 'package:flutter/material.dart';

class CategoriesM extends StatelessWidget {
  const CategoriesM({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Text("category")],);
  }
}